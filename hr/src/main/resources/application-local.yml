spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/msa_hr_db
    username: msa_user
    password: msa_password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
