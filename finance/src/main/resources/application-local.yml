server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:54320/msa_finance_db
    username: msa_user
    password: msa_password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
